{
  "name": "draymih-law-office",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "public"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names BACKEND,FRONTEND --prefix-colors blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "wait-on http-get://127.0.0.1:5000/api/health && npm --prefix public run dev",
    "postinstall": "npm run prisma:generate",
    "prisma:generate": "prisma generate --schema=prisma/schema.prisma",
    "build": "npm run prisma:generate && npm run build:backend && npm run build:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix public run build",
    "start": "npm --prefix backend run start",
    "preview:frontend": "npm --prefix public run preview",
    "db:seed": "npm --prefix backend run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prisma": "^5.22.0",
    "wait-on": "^9.0.10"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}